import PrivateChefPage from '@/pages/services/PrivateChefPage';

/** Route component — /oahu/private-chef (Oʻahu binding of the private chef template). */
export default function OahuPrivateChef() {
  return <PrivateChefPage island="oahu" />;
}
