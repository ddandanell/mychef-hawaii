import PrivateChefPage from '@/pages/services/PrivateChefPage';

/** Route component — /maui/private-chef (Maui binding of the private chef template). */
export default function MauiPrivateChef() {
  return <PrivateChefPage island="maui" />;
}
