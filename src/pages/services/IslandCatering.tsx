import CateringPage from '@/pages/services/CateringPage';

/** Catering / events on every island host — bookable, not coming soon. */
export default function IslandCatering() {
  return <CateringPage />;
}
